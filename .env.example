# Environment Variables Example
# Copy this to .env.local in each worker directory and fill in with your values

# ============================================================================
# domain-a-tools-worker/.env.example
# ============================================================================

# Shared secret between gateway and domain workers
# Generate: openssl rand -hex 32
DOMAIN_SHARED_SECRET=dev-secret-123

# ============================================================================
# domain-b-tools-worker/.env.example
# ============================================================================

# Shared secret (must match Domain A and Gateway)
DOMAIN_SHARED_SECRET=dev-secret-123

# ============================================================================
# mcp-gateway-worker/.env.example
# ============================================================================

# Shared secret (must match both domains)
DOMAIN_SHARED_SECRET=dev-secret-123

# Local development URLs (changed during production deployment)
DOMAIN_A_URL=http://localhost:8001
DOMAIN_B_URL=http://localhost:8002

# Comma-separated list of allowed origins (CORS)
ALLOWED_ORIGINS=localhost:8000,localhost:8501,localhost:3000

# ============================================================================
# mcp-client-streamlit/.env.example
# ============================================================================

# Override default gateway URL
MCP_GATEWAY_URL=http://localhost:8000/mcp

# ============================================================================
# SECRETS Setup
# ============================================================================

# These are NOT stored in .env files (for security).
# Instead, use wrangler secret for each worker:

# $ cd apps/domain-a-tools-worker
# $ wrangler secret put DOMAIN_SHARED_SECRET
# (paste: dev-secret-123)

# $ cd apps/domain-b-tools-worker
# $ wrangler secret put DOMAIN_SHARED_SECRET
# (paste: dev-secret-123)

# $ cd apps/mcp-gateway-worker
# $ wrangler secret put DOMAIN_SHARED_SECRET
# (paste: dev-secret-123)

# For production, use stronger secrets and manage via:
# - Cloudflare KV
# - External secrets manager
# - CI/CD secrets

# ============================================================================
# PRODUCTION DEPLOYMENT
# ============================================================================

# Update wrangler.toml [env.production] section with:

# [env.production]
# vars = {
#   DOMAIN_A_URL = "https://domain-a-<hash>.workers.dev",
#   DOMAIN_B_URL = "https://domain-b-<hash>.workers.dev",
#   ALLOWED_ORIGINS = "yourdomain.com,api.yourdomain.com"
# }

# Then deploy with production environment:
# $ wrangler deploy --env production
