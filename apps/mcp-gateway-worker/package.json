{
  "name": "mcp-gateway-worker",
  "version": "0.1.0",
  "description": "MCP Gateway Worker - Routes to domain tools via RPC",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "hono": "^4.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.2.0",
    "wrangler": "^3.25.0"
  }
}
